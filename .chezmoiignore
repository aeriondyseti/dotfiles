# Non-deployable repo files
__ARCHIVE__/
.claude/
CLAUDE.md
README.md
.vector-memory/
.vscode/
scripts/

# Profile-specific ignores
{{ if ne .profile "ubuntu" }}
.config/kde/
{{ end }}

{{ if eq .profile "server" }}
.config/oh-my-posh/
.config/mise/
.config/ghostty/
.config/micro/
.config/claude/
.config/btop/
.config/lazygit/
.local/bin/helper-prompt
{{ end }}

# ── windows profile ──
# The Unix run_once scripts are gated in-content with {{ "{{ if ne .profile \"windows\" }}" }},
# so they are not listed here ( .chezmoiignore does not reliably skip scripts ).
{{ if ne .profile "windows" }}
# Windows-only files — skip on every other profile
Documents/
{{ end }}

{{ if eq .profile "windows" }}
# Unix shell config — skip on the windows profile
# (oh-my-posh config IS deployed on windows — OMP runs natively)
.zshrc
.zprofile
.config/zsh/
.config/ghostty/
.config/micro/
.config/btop/
.config/lazygit/
.config/bat/
.config/gh/
.config/kde/
{{ end }}
